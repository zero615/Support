package com.zero.support.sample;

import com.zero.support.compat.app.SupportActivity;
import com.zero.support.compat.app.SupportViewModel;

public class MainViewModel extends SupportViewModel {
    @Override
    protected void onViewModelCreated(SupportActivity activity) {
        super.onViewModelCreated(activity);
    }
}
